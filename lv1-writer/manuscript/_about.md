# manuscript/

Your plain-text drafts live here, one file per chapter or piece (e.g. `ch01.md`). The
`draft` station writes here; the `inspector` reads from here. Keep prose plain and
source-backed — every factual claim should trace to `../sources/library.md`.
